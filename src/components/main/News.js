function News({ Scrolled, Pos }) {
    console.log('scrolled', Scrolled);
    console.log('pos', Pos);
    return (
        <section id='news' className='myScroll'>
            <h1>News</h1>
        </section>
    );
}

export default News;